PARAMETER LISTADO
ACTI SCRE
CLEA
IF LISTADO
   SET PRIN TO PROCESO.TXT
   SET PRIN ON
ENDIF   
FOR A=1 TO 32
IF EMPTY(PROGRAM(A))
   EXIT
ELSE
   ? STR(A,2)
   ?? "->"
   ?? PROGRAM(A)
ENDIF
NEXT
SET PRIN TO 
SET PRIN OFF
RETURN
