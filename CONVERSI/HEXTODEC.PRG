PARAMETER COSA
STORE 0 TO SUMA
FOR A=1 TO 4
  B=SUBSTR(COSA,A,1)
  IF VAL(B)=0 .AND. B#"0"
     B=ASC(B)-55
  ELSE
     B=VAL(B)
  ENDIF
  IF B#0
     B=B*16**(4-A)
     SUMA=SUMA+B
  ENDIF
NEXT
RETURN(SUMA)