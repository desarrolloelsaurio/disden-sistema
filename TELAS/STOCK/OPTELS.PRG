***********************************
*-- Programa: OPTELS.PRG
*-- Fecha   : 05/28/91 01:53am
*-- Version : G. REINER
*-- Notas   : ABRE ARCHIVO TELAS PARA STOCK
***********************************
#DEFINE BOTOM  "  "+repl("°",78)                 && BOTOM
#DEFINE ABA    "Abriendo Base de "               && ABA
PARAMETER A_REOP,A_EXCL
IF A_REOP
	=REOPXXX("TELS")
ENDIF
IF USED("TELS")
	IF A_EXCL
		USE IN TELS
		USE (PATHPRO+"\TELSTOCK") ORDER TAG TELCODPRO ALIAS TELS EXCL
	ELSE
		SELE TELS
	ENDIF
ELSE
	=MEN_LIN(ABA+TELLAS)
	=PRXAREA(.T.)
	IF A_EXCL
		USE (PATHPRO+"\TELSTOCK") ORDER TAG TELCODPRO ALIAS TELS EXCL
	ELSE
		USE (PATHPRO+"\TELSTOCK") ORDER TAG TELCODPRO ALIAS TELS
	ENDIF
ENDIF
RETURN IIF(TELPOP,DEFPOGRL(2,30,"TELSPOP","TELS.TELA",40,'Elija '+TELLA),.T.) .AND. ;
	MEN_LIN(BOTOM)
