*
*                          DEPURACION DE ARCHIVOS
*
* DEPURA.PRG
* GOYO / OCT 1993
IF CONFIRM1("REALIZAR DEPURACION DE ARCHIVOS")
   IF PREGUNT1("ESTA SEGURO DE QUERER DEPURAR LOS ARCHIVOS")
      WAIT "TRABAJANDO SOBRE ARCHIVO DE BALANCE..." WIND NOWA
      SELE BAL
      SET ORDER TO
      pack
      sort on CODIGO to TEMPO
      zap
      append from TEMPO
      erase TEMPO.DBF
      WAIT "TRABAJANDO SOBRE ARCHIVO DE MOVIMIENTOS..." WIND NOWA
      SELE MOV
      SET ORDER TO
      pack
      sort on PERIODO,ASIENTO,MOVIMIENTO to TEMPO
      zap
      append from TEMPO
      erase TEMPO.DBF
      WAIT CLEA
      DO RESTAURA
      DO RECONSTR WITH .T.
   ENDIF
ENDIF
=MEN_LIN(SYSNOMBRE)
return
