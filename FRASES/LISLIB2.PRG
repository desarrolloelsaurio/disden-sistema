* LISTA LIBROS SEGUN EL AUTOR
WAN=WONTOP()
=WIN0()
ALA=ALIAS()
ELAUT=AUT.CODIGO
SELE LIB
ORD=ORDER()
SET ORDER TO 
STORE 0 TO REN
CLEA
SCAN FOR LIB.AUTOR1=ELAUT
? LIB.LIBRO
REN=REN+1
   IF REN=23
      IF INKEY(0,"HM")=27
         EXIT
      ELSE
         CLEA
         STORE 0 TO REN
      ENDIF
   ENDIF
ENDSCAN
IF LAST()#27
=INKEY(0,"HM")
ENDIF
IF !EMPTY(ORD)
SET ORDER TO TAG (ORD)
ENDIF
RELE WIND WIN0
IF !EMPTY(WAN)
ACTI WIND (WAN)
ENDIF
SELE (ALA)
RETURN