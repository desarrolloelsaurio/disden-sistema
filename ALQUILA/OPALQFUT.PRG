* ษออออออออออออออออออออออออออออออป
* บ PROCEDIMIENTO : OPALQFUT.PRG บ
* บ COMENTARIO    :              บ
* ศออออออออออออออออออออออออออออออผ
PARAMETER ABRE_AMBOS
#DEFINE BOTOM  "  "+repl("ฐ",78)                 && BOTOM
#DEFINE ABA    "Abriendo Base de "               && ABA
=MEN_LIN(ABA+'Contratos a Futuro')
IF ABRE_AMBOS
   =OPALQFU1("FUT")
ELSE
  IF USED("ALQ")
	 USE IN ALQ
	 =OPALQFU1("ALQ")
	 ENDIF
  ENDIF
IF INMPOP
	=DEFPOGRL(INMPOPR,INMPOPC,"INMPOP","INM.DOMICILIO",40)
ELSE
	RELE POPU INMPOP
ENDIF
RETURN MEN_LIN(BOTOM)

FUNCTION OPALQFU1
	PARAMETER OP_ALIAS
	IF USED(OP_ALIAS)
		SELE OP_ALIAS
		SET ORDER TO TAG CODINM
	ELSE
		SELE 2
		USE (PATHALQ+'\CONTRFUT') ORDER TAG CODINM ALIAS (OP_ALIAS)
	ENDIF
RETURN .T.
