=WORKING()
=DOTALK()
SELE ALQ
TMP=GENTMP()
SELE ALQ
=FLOCK() .OR. FLOK()
DO CASE
CASE ALQ.RECIBO
	TT="Listado de inmuebles con recibo Bancario"
	SORT ON CODINM TO (TMP) FOR RECIBO
OTHERWISE
	TT="Listado de inmuebles sin recibo Bancario"
	SORT ON CODINM TO (TMP) FOR !RECIBO
ENDCASE
acla=PAIP+;
	padc("CODIGO",10)+PAIP+;
	PADC("DOMICILIO",30)+PAIP+;
	PADC("PROPIETARIO",30)+PAIP
SELE ALQ
USE (TMP) ALIAS ALQ
=UNTALK()
=UNWORK()
=LGRL2(PORPAN,"ALQ","LALQREC0()","SIN RECIBO","LALQREC")
RETURN REOPALQ()
