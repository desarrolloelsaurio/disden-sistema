PARAMETER PORPAN
#DEFINE PAIP   "|"                               && PAIP
=WORKING()
=DOTALK()
SELE MIX
SET ORDER TO TAG GASCOD
TMP=GENTMP()
=FLOCK() .OR. FLOK()
COPY TO (TMP)
UNLO IN MIX
=PRXAREA(.T.)
USE (TMP) ALIAS TMP
REPL ALL TEXTO2 WITH STRTRAN(TEXTO2,".","")
REPL ALL NU5 WITH VAL(TEXTO2)
SCAN
ELCOD=ALLTRIM(TEXTO2)
CEROS=0
LARG=LEN(ELCOD)
FOR XA=LARG TO 1 STEP -1
   IF SUBSTR(ELCOD,XA,1)="0"
      CEROS=CEROS+1
   ELSE
   EXIT
   ENDIF
NEXT   
REPL NU1 WITH 7-CEROS
ENDSCAN
REPL ALL TEXTO2 WITH TRANS(TEXTO2,"@R 99.99.9")
TT="Plan de Cuentas"
ACLA=PAIP+;
PADC("CODIGO",15)+;
PAIP+;
PADC("CUENTA",28)+;
PAIP
=UNTALK()
=UNWORK()
=LGRL2(PORPAN,"TMP","LGAS0()","","LGAS")
RETURN OFF_TMP() .AND. MEN_LIN(BOTOM)