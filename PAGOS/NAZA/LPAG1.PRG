FUNCTION LPAG1
	EXTERNAL ARRAY LLIN, LLINT
	IF POR_DIA
		MM = FECHAVE-M.FECINI+1
	ELSE
		MM = MONGOTH(M.FECINI, FECHAVE)+1
	ENDIF
	STORE MONTO+LLIN[MM] TO LLIN[MM]
	STORE MONTO+LLINT[MM] TO LLINT[MM]
RETURN .T.
