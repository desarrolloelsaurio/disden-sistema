*DO INGPRS.SPR WITH "A"
DO WHILE .T.
	DEFI WIND AS FROM 10,30 TO 14,51 DOUBLE COLO SCHE 8
	ACTI WIND AS
	@ 1,2 SAY "TIPO : " GET A DEFA "AVIO  " PICT "@M AVIO ,TELA ,COMPRA, OTROS, PROVEE,DOCUMU"
	READ
	RELE WIND AS
	IF LAST()=27
		EXIT
	ENDIF
	DO LPRS WITH LEFT(A,1)
ENDDO
RETURN