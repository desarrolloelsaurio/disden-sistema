***********************************
*-- Programa: OPAVIS.PRG
*-- Fecha   : 05/28/91 01:53am
*-- Version : G. REINER
*-- Notas   : ABRE ARCHIVO AVIOS PARA STOCK
***********************************
#DEFINE BOTOM  "  "+repl("°",78)                 && BOTOM
#DEFINE ABA    "Abriendo Base de "               && ABA
PARAMETER A_REOP,A_EXCL
IF A_REOP
	=REOPXXX("AVIS")
ENDIF
IF USED("AVIS")
	IF A_EXCL
		USE IN AVIS
		USE (PATHPRO+"\AVISTOCK") ORDER TAG AVICODPRO ALIAS AVIS EXCL		
	ELSE
		SELE AVIS
	ENDIF
ELSE
	=MEN_LIN(ABA+abios)
	=PRXAREA(.T.)
	IF A_EXCL
		USE (PATHPRO+"\AVISTOCK") ORDER TAG AVICODPRO ALIAS AVIS EXCL
	ELSE
		USE (PATHPRO+"\AVISTOCK") ORDER TAG AVICODPRO ALIAS AVIS
	ENDIF
ENDIF
RETURN IIF(AVIPOP,DEFPOGRL(AVIPOPR,AVIPOPC,"AVISPOP","AVIS.AVIO",40,'Elija '+ABIO),.T.) .AND. ;
MEN_LIN(BOTOM)
