?trazable("0000000050")
?trazable("0000109497")
?trazable("0000000050","MA")
?trazable("0000109497","MA")
return


FUNCTION XSEEK
PARAMETER QUEBUSCA,APODO,HORDEN,X_NEAR
PRIVATE QUEBUSCA,APODO,HORDEN,SALIDA,APODOANT,HORDENANT
IF PARAMETER()<2
   M.SALIDA=.F.
ELSE
   STORE ALIAS() TO M.APODOANT
   SELE (M.APODO)
   IF PARAMETER()=3
	  M.ORDENANT=ORDER()
	  SET ORDER TO TAG (M.HORDEN)
   ENDIF
   IF X_NEAR
      SET NEAR ON
   ENDIF
   M.SALIDA=SEEK(M.QUEBUSCA)
   IF X_NEAR
      SET NEAR OFF
   ENDIF
   IF PARAMETER()=3
	  SET ORDER TO TAG (M.ORDENANT)
   ENDIF
   IF !EMPTY(M.APODOANT)
      SELE (M.APODOANT)
   ENDIF
ENDIF
RETURN M.SALIDA



DO ..\..\HERRAM\BUSCAPJX WITH "SEEKTECX(40"

fisc="articulo='0000105025'"
DEFI WIND WIN FROM 5,5 TO WROWS()-2,WCOLS()-2 TITL "MOD. PRECIOS "+TT ;
FONT "ARIAL",10
BROW FIEL ARTICULO:P="!!!!!!!!!!":R:16 ,;
MERCA:R:H=PADC("DESCRIPCION",35):P=REPL("!",35):R ,;
MER.FOB:H="F.O.B.":P="9999.99":R ,;
MER.NACION:H="% NAC.":P="999.99" ,;
MER.MONEDA:H="MO":V=(MONEDA=" " .OR. MONEDA="E" .OR. MONEDA="Y" .OR. MONEDA="F" .OR. MONEDA="D"):E="Solo Euro, Dolar, Franco Suizo o Yen";
FOR &FISC ;
WIND WIN ;
FONT "ARIAL",9 
