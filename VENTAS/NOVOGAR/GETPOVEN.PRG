FUNCTION GETPOVEN
IF GETENV("EQUIPO")="ARROYO" .OR. ;
		GETENV("EQUIPO")="NOVOLAP"  .OR. ;
		GETENV("EQUIPO")="GOYOLAP"  .OR. ;
		GETENV("EQUIPO")="EQUIPA"
   REST FROM C:\SISTEMAS\VENTAS\VENTASP\POSIC ADDI
ELSE
   IF GETENV("EQUIPO")="MICASA"
      REST FROM D:\SISTEMAS\VENTAS\NOVOGAR\POSIC ADDI
   ELSE
      REST FROM ("F:\USR\"+GETENV("USERNAME")+"\POSICVEN.MEM") ADDI
   ENDIF
ENDIF
RELE ALL LIKE XA*
RELE ALL LIKE PATH*
RETURN GETPATH()
