FUNCTION LPACCON0
	#DEFINE PAIP   "|"                             && PAIP
	? PAIP
	?? LIQ.TPR
	?? PAIP
	?? LIQ.NUMERO
	?? PAIP
	?? LIQ.PACIENTE
	?? PAIP
	?? LEFT(PER.CLIENTE,IIF(PORPAN,20,30))
	?? PAIP
	IF EMPTY(PER.TEXTO1)
		?? "FALTA OBRA SOCIAL       "
	ELSE
		IF EMPTY(PER.TEXTO3)
			?? "FALTA NUMERO DE AFILIADO"
		ELSE
			IF EMPTY(LIQ.PRACTICA)
				?? "FALTA PRACTICA          "
			ELSE
				IF EMPTY(LIQ.ORDEN)
					?? "FALTA NUMERO DE ORDEN   "
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	?? PAIP
RETURN .T.
