FUNCTION LPACCON2
	#DEFINE PAIP   "|"                             && PAIP
	? PAIP
	?? LIQ.TPR
	?? PAIP
	?? LIQ.NUMERO
	?? PAIP
	?? LIQ.PACIENTE
	?? PAIP
	?? LEFT(PER.CLIENTE,IIF(PORPAN,20,30))
	?? PAIP
	IF EMPTY(LIQ.ORDEN)
		?? "FALTA NUMERO DE ORDEN   "
	ELSE
		IF EMPTY(LIQ.MEDICO)
			?? "FALTA MEDICO DERIVADOR  "
		ELSE
			IF EMPTY(EMPTY(LIQ.INTERNO))
				?? "FALTA TECNICO           "
			ENDIF
		ENDIF
	ENDIF
	?? PAIP
RETURN .T.
