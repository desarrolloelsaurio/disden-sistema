* ÚÄ Funci¢n  ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
* ³  Description: LPECPZ1    ³
* ³       Author: G. REINER  ³
* ³ Date created: 10-15-92   ³
* ³ Time created: 06:30:12pm ³
* ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
#DEFINE NUL    0                                 && NUL
#DEFINE PAIP   "|"                               && PAIP

FUNCTION LPECPZ1
	? PAIP
	=IIF(VARIABLE[1],QOUT(PEDIDO+PAIP),'')
	=IIF(VARIABLE[2],QOUT(DTOC(FECHAEM)+PAIP),'')
	=IIF(VARIABLE[3],QOUT(LEFT(PRO.PROVEEDOR,TEXTO[3,2])+PAIP),"")
	IF VARIABLE[4]
		=XSEEK(CONDPAGO1,'MIX','PAGCOD')
		?? LEFT(MIX.TEXTO1,TEXTO[4,2])
		?? PAIP
	ENDIF
	=IIF(VARIABLE[5],QOUT(STR(PRESUP1,TEXTO[5,2],2)+PAIP),"")
	IF VARIABLE[6]
		=XSEEK(CONDPAGO2,'MIX','PAGCOD')
		?? LEFT(MIX.TEXTO1,TEXTO[6,2])
		?? PAIP
	ENDIF
	=IIF(VARIABLE[7],QOUT(STR(PRESUP2,TEXTO[7,2],2)+PAIP),"")
	IF VARIABLE[8]
		=XSEEK(CONDPAGO3,'MIX','PAGCOD')
		?? LEFT(MIX.TEXTO1,TEXTO[8,2])
		?? PAIP
	ENDIF
	=IIF(VARIABLE[9],QOUT(STR(PRESUP3,TEXTO[9,2],2)+PAIP),"")
	=IIF(VARIABLE[10],QOUT(DTOC(FECHAENT)+PAIP),"")
	=IIF(VARIABLE[11],QOUT(DTOC(FECHAPROM)+PAIP),"")
	CASOS=CASOS+1
RETURN .T.
