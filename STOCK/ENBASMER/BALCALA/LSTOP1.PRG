FUNCTION LSTOP1
? PAIP
=IIF(VARIABLE[1],QOUT(LEFT(ARTICULO,TEXTO[1,2])+PAIP),'')
=IIF(VARIABLE[2],QOUT(LEFT(MER.MERCA,TEXTO[2,2])+PAIP),'')
IF VARIABLE[3]
	?? STR(MER.STOCK,XACDIGI,2)
	?? PAIP
	TTL[3]=TTL[3]+MER.STOCK
	PTL[3]=PTL[3]+MER.STOCK
ENDIF
IF VARIABLE[4]
	?? STR(MER.STOCKMIN,XACDIGI,2)
	?? PAIP
	TTL[4]=TTL[4]+MER.STOCKMIN
	PTL[4]=PTL[4]+MER.STOCKMIN
ENDIF
IF VARIABLE[5]
	?? STR(MER.STOCKMAX,XACDIGI,2)
	?? PAIP
	TTL[5]=TTL[5]+MER.STOCKMAX
	PTL[5]=PTL[5]+MER.STOCKMAX
ENDIF
IF VARIABLE[6]
	?? STR(MER.STOCKRES,XACDIGI,2)
	?? PAIP
	TTL[6]=TTL[6]+MER.STOCKRES
	PTL[6]=PTL[6]+MER.STOCKRES
ENDIF
IF VARIABLE[7]
	?? STR(MER.STOCKPES,XACDIGI,2)
	?? PAIP
	TTL[7]=TTL[7]+MER.STOCKPES
	PTL[7]=PTL[7]+MER.STOCKPES
ENDIF
IF VARIABLE[8]
	?? STR(EVAL(RESTA),XACDIGI,2)
	?? PAIP
	TTL[8]=TTL[8]+EVAL(RESTA)
	PTL[8]=PTL[8]+EVAL(RESTA)
ENDIF
IF VARIABLE[9]
	NUMERO=MER.COSTO*IIF(M.HAYDISC .AND. MER.DISCRIM,MER.STOCKPES,MER.STOCK)
	?? STR(NUMERO,XACDIGI,2)
	?? PAIP
	TTL[9]=TTL[9]+NUMERO
	PTL[9]=PTL[9]+NUMERO
ENDIF
CASOS=CASOS+1
RETURN .T.
