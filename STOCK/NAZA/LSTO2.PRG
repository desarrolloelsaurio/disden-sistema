FUNCTION LSTO2
	? PAIP
	=iif(variable[1],qout(left(LAMERCA,texto[1,2])+paip),'')
	=iif(variable[2],qout(left(LAMERCA1,texto[2,2])+paip),'')
	IF VARIABLE[3]
		?? STR(MTL[3],XACDIGI)
		?? PAIP
	ENDIF
	IF VARIABLE[4]
		?? STR(MTL[4],XACDIGI)
		?? PAIP
	ENDIF
	IF VARIABLE[5]
		?? STR(MTL[5],XACDIGI)
		?? PAIP
	ENDIF
	IF VARIABLE[6]
		?? STR(MTL[6],XACDIGI)
		?? PAIP
	ENDIF
	IF VARIABLE[7]
		?? STR(MTL[7],XACDIGI)
		?? PAIP
	ENDIF
	IF VARIABLE[8]
		?? STR(MTL[8],XACDIGI)
		?? PAIP
	ENDIF
	LAMERCA=IIF(FILXXX=1,LEFT(MER.ARTICULO,10),IIF(FILXXX=2,LEFT(MER.ARTICULO,13),SUBSTR(MER.ARTICULO,5,6)))
	LAMERCA1=MER.MERCA
	STORE NUL TO MTL
RETURN .T.
