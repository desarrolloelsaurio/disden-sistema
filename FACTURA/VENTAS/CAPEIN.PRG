   FECINI = TO_DATE("01"+SUBSTR(TO_CHAR(FECHA FACTURA),4,7)
   SELECT SUM(M.MONTONET), SUM(M.MONTOBRU), SUM(M.MONTOOT3) FROM FACTURA F;
    WHERE F.FECHAEM BETWEEN FECINI AND FECFIN AND F.CODCLI = CODIGO CLIENTE
     INTO TMONTONET, TMONTOBRU, TMONTOOT3
   IF M.