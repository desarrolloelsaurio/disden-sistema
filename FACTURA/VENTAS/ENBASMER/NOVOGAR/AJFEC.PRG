FUNCTION AJFEC
SELE CUOTAS
STORE RECNO() TO ELREC
STORE .T. TO M.AJFEC
STORE {} TO M.FOR
SCAN
   IF M.FOR<FECHAVE
      STORE FECHAVE TO M.FOR
   ELSE
      STORE .F. TO AJFEC
      EXIT
   ENDIF
ENDSCAN
GO (ELREC)
LADIF=CUOTAS.FECHAVE-CUOTAS.FVE_OK
SCAN WHILE !EOF()
REPL CUOTAS.FECHAVE WITH CUOTAS.FVE_OK+LADIF
ENDSCAN
GO (ELREC)
RETURN AJFEC
