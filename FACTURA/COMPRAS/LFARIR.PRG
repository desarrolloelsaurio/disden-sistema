********************************************************
*-- Programa: LFARIR.PRG
*-- Fecha   : 10-16-92 04:51pm
*-- Version : G. REINER
*-- Notas   : LISTADO DE FACTURAS DE COMPRA (RESUMENES)
********************************************************
STORE HORA_ULP(program()) TO ULPR
=CHEQSEG()
STORE NUL TO CASOS,LINEA,CAN,TOT,TCAN,TTOT,ELARTICULO,LAMERCA,ELCORTE,ELTEXTO
=LFARPR0()
=CABPRI()
_PCOPIES=1
PRINTJOB
=CABPRI1()
=TIT_LIN(3)
SCAN
	=LFARPR1()
	IF _PLINENO>63
		IF FILFAC=3
			=FIN_LIN(3,paip+"SUBTOTAL"+PAIP+SPACE(14)+PAIP+SPACE(20)+PAIP+;
				STR(TOT,XACDIGI,2)+PAIP,CASOS)
		ELSE
			=!FIN_LIN(3,PAIP+"SUBTOTAL"+PAIP+;
				STR(TCAN,XACDIGI)+PAIP+STR(TTOT,XACDIGI,2)+PAIP,CASOS)
		ENDIF
		=cabpri1()
		=TIT_LIN(4)
	ENDI
	=LFARPR2()
ENDSCAN
=LFARPR1()
IF FILFAC=3
	=FIN_LIN(4,paip+PADC("TOTAL",8)+PAIP+SPACE(14)+PAIP+SPACE(20)+PAIP+;
		STR(TOT,XACDIGI,2)+PAIP,CASOS)
ELSE
	=!FIN_LIN(4,PAIP+"SUBTOTAL"+PAIP+;
		STR(TCAN,XACDIGI)+PAIP+STR(TTOT,XACDIGI,2)+PAIP,CASOS)
ENDIF
EJECT PAGE
ENDP
=FINIMP()
=IAUD(FISC)
RETURN MEN_LIN(BOTOM)
