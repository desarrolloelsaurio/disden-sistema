FUNCTION LP_TORUB
	PARAMETER LP_TEXTO
	EXTERNAL ARRAY TOTLIN,TOTRUB,TOTTOT
	STORE NUL TO TTL
	T_TEX=PAIP+;
		PADR(LP_TEXTO,IIF(LG_PORP,10,41))+;
		PAIP
	FOR X=1 TO MAXETA
		T_TEX=T_TEX+STR(TOTRUB[X],XACDIGI)+PAIP
		TTL=TTL+TOTRUB[X]
	NEXT
	T_TEX=T_TEX+STR(TTL,XACDIGI)+PAIP
RETURN T_TEX
