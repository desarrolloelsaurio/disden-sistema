FUNCTION LFABNT1
#DEFINE PAIP   "|"                               && PAIP
? PAIP
=IIF(VARIABLE[1],QOUT(STR(NUMERO)+PAIP),"")
=IIF(VARIABLE[2],QOUT(REMITO+PAIP),"")
=IIF(VARIABLE[3],QOUT(LEFT(ARTICULO,TEXTO[3,2])+PAIP),"")
=IIF(VARIABLE[4],QOUT(LEFT(MERCA,TEXTO[4,2])+PAIP),"")
=IIF(VARIABLE[5],QOUT(LEFT(CLIENTE,TEXTO[5,2])+PAIP),"")
=IIF(VARIABLE[6],QOUT(DTOC(EF)+PAIP),"")
	IF VARIABLE[7]
       ?? STR(EEC,XACDIGI)
       ?? PAIP
       ttl[7]=ttl[7]+EEC
	   CTRL[7]=.T.
    ENDIF
=IIF(VARIABLE[8],QOUT(STR(DATE()-EF,XACDIGI)+PAIP),"")
STORE CASOS+1 TO CASOS
RETURN .T.
