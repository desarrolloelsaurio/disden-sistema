#DEFINE CRLF CHR(13)+CHR(10)                     && CRLF
#DEFINE PAIP   "|"                               && PAIP
=WORKING()
=DOTALK()
SET ORDER TO TAG NUMERO IN CO1
SET ORDER TO TAG NUMERO IN COR
SET ORDER TO TAG TALCOD IN PER
SET ORDER TO TAG PRCCOD IN PRC
SET ORDER TO TAG ARTIRED IN MER
SELE COR
SET RELA TO NUMERO INTO CO1
SET RELA TO ARTICULO INTO MER ADDI
SELE FAX
SET RELA TO NUMERO INTO COR ADDI
SET RELA TO PRC INTO PRC ADDI
SET RELA TO TALLER INTO PER ADDI
SET ORDER TO
TMP=GENTMP()
FISC="EMPTY(FAX.SF) .AND. !EMPTY(FAX.EF)"
TT1="TODAS LAS ORDENES"
=FABNTZ()
SORT ON PER.CLIENTE,COR.ARTICULO TO (TMP) FOR ;
  &FISC ;
  FIELD PER.CLIENTE,;
  PRC.PROCFAB,;
  FAX.PRC,;
  COR.ARTICULO,;
  NUMERO,;
  REMITO,;
  MER.MERCA,;
  EF,;
  EEC
SELE FAX
USE (TMP) ALIAS FAX EXCL
TT="LISTADO DE ORDENES DE CORTE"+CRLF+;
  "PROCEDOS NO TERMINADOS"+CRLF+;
  TT1+CRLF+;
  "POR TALLER"
ACLA=PAIP+;
  PADC("NUMERO",10)+PAIP+;
  IIF(LG_PORP,"",PADC("REMITO",15)+PAIP)+;
  PADC("ARTICULO",10)+PAIP+;
  PADC("DENOMINACION",IIF(LG_PORP,15,30))+PAIP+;
  PADC("PROCESO",IIF(LG_PORP,15,30))+PAIP+;
  " FECHA  "+PAIP+;
  PADC("CANTIDAD",XACDIGI-2)+PAIP+;
  PADC("DIAS",XACDIGI-2)+PAIP
LINEA=REPL("=",LEN(ACLA))
STORE .F. TO NOFIRST
STORE "" TO ELPRC,ELPRCTEX
STORE NUL TO CASOSPARC,TTPARC,CASOS,TTTOT,PAG
SELE FAX
GO TOP
=FABNTtt0()
=UNWORK()
=UNTALK()
=IIF(LG_PORP,FABNTTTP(),FABNTTTI())
RETURN REOPFAB()
