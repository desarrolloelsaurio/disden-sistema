FUNCTION LDES00
	? PAIP
	?? AVI
	?? PAIP
	IF !PORPAN
		IF TIPO="A"
			?? SAYGRL(TMP.AVI,"AVI","AVICOD","AVI.AVIOS")
		ELSE
			?? SAYGRL(TMP.AVI,"TEL","TELCOD","TEL.TELA")
		ENDIF
		?? PAIP
	ENDIF
	?? TMP.AVICODPRO
	?? PAIP
	?? TMP.TELCODPRO
	IF !PORPAN
		?? PAIP
		IF TIPO="A"
			?? SAYGRL(TMP.AVICODPRO,"AVIS","AVICODPRO","AVIS.AVIO")
		ELSE
			?? SAYGRL(TMP.TELCODPRO,"TELS","TELCODPRO","TELS.TELA")
		ENDIF
	ENDIF
	?? PAIP
	?? STR(CANTIDAD,10,4)
	?? PAIP
RETURN

