* ษอออออออออออออออออออออออออออออออออออออออออออออออออออออออป
* บ PROCEDIMIENTO : LPRE0.PRG                             บ
* บ COMENTARIO    : GENERA CABECERA PARA LISTA DE PRECIOS บ
* ศอออออออออออออออออออออออออออออออออออออออออออออออออออออออผ
FUNCTION LPRE0
=DOTALK()
	TMP=GENTMP()
	SET ORDER TO TAG TODOS IN RUB
	=FLOCK("RUB") .OR. FLOK("RUB")
	SET ORDER TO TAG MARCOD IN MI1
	=FLOCK("MI1") .OR. FLOK("MI1")
	SELE MER
	SET ORDER TO
	=FLOCK() .OR. FLOK()
	SET RELA TO STR(RUBRO)+STR(SUBRUBRO) INTO RUB
	SET RELA TO MARCA INTO MI1 ADDI
	LISTACAMPO=LISTACPO('RUB.PALISTAR,MI1.TEXTO1,MER.COSTO,')
	IF PORPAN
		SORT ON RUB.PALISTAR,ARTICULO ;
		TO (TMP) ;
		FOR &FISC ;
		FIELDS &LISTACAMPO
	ELSE
		SORT ON RUB.PALISTAR,MI1.TEXTO1,ARTICULO ;
		TO (TMP) ;
		FOR &FISC ;
		FIELDS &LISTACAMPO
	ENDIF
	UNLO ALL
	USE IN MER
	=PRXAREA(.T.)
	USE (TMP) ALIAS MER EXCL
	SELE MER
	=UNTALK()
RETURN UNWORK()
