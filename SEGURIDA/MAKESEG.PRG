PROCEDURE MAKESEG
	PRIVATE DRIVE,A,CANTIDAD,messG,PREVESC,PREVDELE,PREVPATH,PREVTALK
	***** messages ***********************
	dime MESSG[5]
	MESSG[1]='PROJECT'
	MESSG[2]='DRIVE'
	MESSG[3]='DIRECTORY'
	MESSG[4]='SOURCE PROJECT TO TRANSFER'
	MESSG[5]=' NOW TRANSFERING '
	*************************************
	set talk off
	set esca on
	SET DELE ON
	SET PATH TO \SISTEMAS\HERRAM,\SISTEMAS\VENTANAS
	clea
	DEFI WIND WIN FROM 0,0 TO 6,70 DOUB
	=CENTWIN("WIN")
	@ 1,1 SAY MESSG[1]
	@ $+1,1 SAY MESSG[2]
	@ $+1,1 SAY MESSG[3]
	proyecto=getfile("pjx",MESSG[4])
	IF !EMPTY(PROYECTO) .AND. LAST()#27
CREATE DBF SEGURIDA.DBF ;
(PROGRAMA C(10),;
 SISTEMA C(10),;
 CSEG N(4,0),;
 FECHA D,;
 USUARIO C(10))
 CLOSE DATA
SELE 1
USE SEGURIDA.DBF ALIAS SEG
SELE 2
	   USE (PROYECTO) ALIAS PRO 
	   M.CSEG=10
	   SCAN
	   IF 'PRG' $ NAME
	   M.PROGRAMA=SUBSTR(PRO.NAME,RAT('\',PRO.NAME)+1)
	   M.PROGRAMA=LEFT(M.PROGRAMA,RAT('.',M.PROGRAMA)-1)
	   M.FECHA=DATE()
	   M.USUARIO='GOYO'
	   INSERT INTO SEG FROM MEMV
	   ENDIF
	   ENDSCAN
	ENDIF
	RELE WIND WIN
	SET PATH TO
	CLEA
RETURN
