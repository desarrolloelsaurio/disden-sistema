PROCEDURE OFF_IDX
PRIVATE OFF_A,OFF_B,OFF_BASE
WAIT "BORRANDO INDICES TEMPORALES" WIND NOWA
OFF_BASE=ALIAS()
FOR OFF_A=1 TO SELE(1)
  IF RIGHT(NDX(1,OFF_A),3)='TMP'
     SET INDEX TO 
  ENDIF
NEXT
IF !EMPTY(OFF_BASE)
   SELE (OFF_BASE)
ENDIF
WAIT CLEA
RETURN
