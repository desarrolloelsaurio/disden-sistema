#DEFINE PAIP   "|"                               && PAIP
FUNCTION HAZ_ACLA
PARAMETER HAZ_TERCIO
	AC_SALIDA=PAIP
	FOR A=1 TO CANT_MAX
		STORE AC_SALIDA+;
		IIF(VARIABLE[A],;
		PADC(ALLTRIM(TEXTO[A,1]),;
		IIF(HAZ_TERCIO,TEXTO[A,3],TEXTO[A,2]))+;
		PAIP,'') TO AC_SALIDA
	NEXT
RETURN AC_SALIDA
