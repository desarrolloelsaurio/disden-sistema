function remp2
	sele tmp
	STORE 0 TO CANREAL,PUNTERO
	scan
		=XSEEK(LEFT(TMP.ARTICULO,10),'MER','ARTICULO')
		@ REN,C1 SAY LEFT(MER.ARTICULO,10)
		@ REN,C2 SAY LEFT(MER.MERCA,23)
		TOtTALG=TALLE1+TALLE2+TALLE3+TALLE4+TALLE5+TALLE6
		@ REN,C4 SAY TOTTALG PICT '999999'
		store PORCEN(MER.COSTO,IIF(M.CLIPRE=1,MER.precio1,IIF(M.CLIPRE=2,MER.precio2,IIF(M.CLIPRE=3,MER.precio3,MER.precio4)))) to unix,unitario
		FINAL=ROUND(UNITARIO,2)
		@ REN,C5 SAY final PICT "99999999.99"
		PARCIAL=FINAL*TOTTALG
		@ REN,C6 SAY PARCIAL PICT PICU
		=MEN_LIN("ESPERE POR FAVOR")
		PRETOT=PRETOT+PARCIAL
		CANART=CANART+TOTTALG
		@ 8,C6 SAY PRETOT PICT PICU
		@ 8,C4 SAY CANART PICT '99999999'
		FOR B=1 TO 6
			VAR='TALLE'+ALLTRIM(STR(B))
			if !empty(EVAL(VAR))
				puntero=puntero+1
				DIME FAXT[PUNTERO,11]
				FAXT[PUNTERO,1]=NADREM
				FAXT[PUNTERO,2]=1
				FAXT[PUNTERO,3]=.F.
				FAXT[PUNTERO,4]=left(mer.articulo,10)+'/'+SUBSTR(MER.ARTICULO,12,2)+zero(b*10,3)
				FAXT[PUNTERO,5]=NUL
				FAXT[PUNTERO,6]=NUL
				faxt[puntero,7]=B
				FAXT[PUNTERO,8]=EVAL(VAR)
				faxt[puntero,9]=SUBSTR(MER.ARTICULO,12,2)
				=repuse1(@faxt,puntero,10)
			endif
		next
		REN=REN+1
		IF REN>22
			=CUADRO("4 7 23 0 1 16 34 11 14")
			@ 8,1 SAY "ARTICULO"
			@ 8,18 SAY "MERCADERIA"
			@ 8,36 SAY "TAL/CUR"
			@ 8,44 SAY "CANTIDAD"
			@ 8,53 SAY "PRECIO UNI."
			@ 8,C6 SAY PRETOT PICT PICU
			REN=10
		ENDIF
	ENDs
return .t.
