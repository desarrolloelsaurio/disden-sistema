FUNCTION LREMPZ0
	=WORKING()
	SELE REM
	SET ORDER TO TAG REMITO
	SET ORDER TO TAG TALCOD IN PER
	SET RELATION TO REM.TALLER INTO PER
	STOR NUL TO CASOS
    LACOSA=GENTMP()
	IF ".T."=FISC
		INDE ON REMITO TO (LACOSA) COMP
	ELSE
		INDE ON REMITO TO (LACOSA) FOR EVAL(FISC) COMP
	ENDIF
	ACLA=PAIP+;
	"FECHA RE"+PAIP+;
	PADC("REMITO",15)+PAIP+;
	PADC("TALLER",30)+PAIP+;
	PADC("O. FABR",10)+PAIP+;
	" USUARIO  "+PAIP
	linea=REPL("=",LEN(acla))
	tt='Listado de Remitos de Producci¢n'+iif(!empty(TTQUE),ttque,"")
	=UNWORK()
RETURN .T.
